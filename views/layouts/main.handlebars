<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{{title}}</title>
  </head>
  <body>
    {{#if flash}}
      <div>
        <strong>{{flash.intro}}</strong> {{{flash.message}}}
      </div>
    {{/if}}
    <nav>
      <ul>
        <li><a href="/">atelier</a></li>
      </ul>
    </nav>
    {{{body}}}
  </body>
</html>
